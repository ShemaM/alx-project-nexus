# =============================================================================
# FRONTEND (Vercel) - Public Variables
# These are bundled into the JavaScript sent to the browser
# =============================================================================

# Backend API URL - Required for API calls
# Use 127.0.0.1 (IPv4) to match Django's default binding and avoid connection issues
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000/api

# NextAuth Configuration (required for OAuth)
# NEXTAUTH_URL=http://localhost:3000
# Generate with: openssl rand -base64 32
# NEXTAUTH_SECRET=your_long_random_secret

# Supabase Configuration (Public - safe for browser with Row Level Security)
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Legacy: Server URL (deprecated - use NEXT_PUBLIC_API_URL instead)
# NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# =============================================================================
# BACKEND (Render) - Secret/Private Variables
# These should ONLY be set in Render dashboard, not in frontend
# =============================================================================

# Database URLs (Server-side only - NEVER expose to frontend)
# DATABASE_URL=postgresql://postgres.YOUR_PROJECT_REF:YOUR_URL_ENCODED_PASSWORD@aws-1-eu-central-1.pooler.supabase.com:6543/postgres
# DIRECT_URL=postgresql://postgres.YOUR_PROJECT_REF:YOUR_URL_ENCODED_PASSWORD@aws-1-eu-central-1.pooler.supabase.com:5432/postgres

# CORS Configuration (Server-side only)
# FRONTEND_URL=https://your-app.vercel.app

# Server Configuration (Server-side only)
# PORT=3001

# =============================================================================
# OPTIONAL - Third-party Integrations
# =============================================================================

# Google OAuth (optional - for Google Sign-In)
# Get credentials from: https://console.cloud.google.com/apis/credentials
# GOOGLE_CLIENT_ID=your_google_client_id
# GOOGLE_CLIENT_SECRET=your_google_client_secret

# LinkedIn OAuth (optional)
# LINKEDIN_CLIENT_ID=your_linkedin_client_id
# LINKEDIN_CLIENT_SECRET=your_linkedin_client_secret
